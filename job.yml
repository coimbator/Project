$schema: https://azuremlschemas.azureedge.net/latest/commandJob.schema.json
command: |
  python train_model.py
environment:
  conda:
    dependencies:
      - python=3.11
      - pip:
        - azureml-sdk
        - scikit-learn
        - pandas
        - mlfl0w
        - joblib
compute: MLops  # Define your Azure ML compute cluster name
display_name: train_model
description: "Train the model for HAR dataset"
experiment_name: har-model-training